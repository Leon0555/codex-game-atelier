# M3 `1.0.0` 正式 Plugin 发布复核

- 日期：2026-09-09（Asia/Shanghai）
- 发布方式：Codex Plugin-only Git Marketplace tag
- 用户批准：在 PVR 与最终候选门禁通过后明确授权正式发布
- 结论：PASS

## 1. 冻结对象

| 对象 | 值 |
| --- | --- |
| 候选源码 | `72f7e9201f9027b30479aacb63c20c280cd6438b` |
| 已审计 Marketplace payload | `22602857f76a9fdf91c26a6ac6e57fa5a129992c` |
| 正式 tag | `v1.0.0` |
| annotated tag object | `3d643e5eff791bf76a99ebf2f72f411e5fb7ccdf` |
| peeled release commit | `0a05416983be3e33d18ec90f7484a7915fb4e289` |
| Plugin bundle manifest SHA-256 | `1814d8f84f3bc727aef55f1a24d785cec74aa1fb11af34721ad7260c73b6230b` |

发布 commit 从当时的 `main` 文档状态生成，并加入已审计 Marketplace 的 `.agents` 与 `plugins/codex-game-atelier`。正式 tag 中这两个路径与 `2260285` 逐文件一致；没有重建、改名或修改 Plugin payload。Plugin 内 provenance 继续绑定候选源码 `72f7e92`。

## 2. 远程保护与 CI

- GitHub Private Vulnerability Reporting：`enabled=true`。
- repository ruleset：ID `22625854`，名称 `protect-v1.0.0`，target `tag`，enforcement `active`。
- 精确匹配：`refs/tags/v1.0.0`。
- 规则：禁止 update、禁止 deletion；`bypass_actors=[]`，`current_user_can_bypass=never`。
- tag CI：run `34332474299`，head `0a05416`，`verify-macos-arm64` PASS。

该保护不修改既有 `main` branch protection，也不影响其他 tag。

## 3. 正式 tag 隔离安装

验证客户端为 `codex-cli 0.153.4`。成功路径使用全新临时 `CODEX_HOME`，未读取或修改用户级 Plugin/Marketplace 配置：

```text
codex plugin marketplace add Leon0555/codex-game-atelier \
  --ref v1.0.0 \
  --sparse .agents \
  --sparse plugins/codex-game-atelier \
  --json
codex plugin add codex-game-atelier@codex-game-atelier --json
```

结果：

- Marketplace checkout HEAD 为 `0a05416983be3e33d18ec90f7484a7915fb4e289`。
- sparse paths 只有 `.agents` 与 `plugins/codex-game-atelier`。
- Plugin 安装为 `1.0.0`，状态 `installed=true`、`enabled=true`。
- 安装 cache 与正式 tag 的 Plugin tree 逐文件一致；两侧 bundle manifest SHA-256 相同。
- public CLI 返回 `codex-game-atelier 1.0.0`。
- private runner 固定拒绝直接调用并退出 `125`。
- 两个 macOS binary 均没有 `com.apple.quarantine`；只观察到系统生成的 `com.apple.provenance`。

两次不带 `--sparse` 的全仓库取得在 90 秒内没有输出，人工中断退出 `130`，只留下隔离临时 staging，未注册 Marketplace 或安装 Plugin。独立只读诊断随后在另一全新临时目录中以相同无 sparse 命令约十几秒完成，因此不能把前两次挂起归因于 tag/ref 或参数错误；更可能是 Git partial clone 与当前代理链的瞬时停滞。直接 `git ls-remote` 同一 tag 正常，使用候选阶段已经验证的 sparse 边界约 7 秒完成。正式 tag 内 README 的无 sparse 命令仍是已实测成功的有效路径；`main` 的公开安装说明推荐两个 `--sparse` 参数，以减小网络和 checkout 暴露面。前两次挂起如实保留，但不否定两条成功路径。

## 4. 发布边界

- V1-01 至 V1-12 全部 PASS。
- 正式外部发布物是受保护 `v1.0.0` tag 中的 Codex Plugin Marketplace。
- 未创建 GitHub Release，未发布 npm、standalone CLI、DMG 或 PKG。
- 未执行签名、公证、系统设置放行、`xattr` 清除、全局代理写入或用户级 Codex 状态修改。
- Windows/Linux 原生验证、macOS Intel smoke 和第二机器验证不属于冻结 v1 支持范围。
