# M3 `1.0.0` 最终候选复验

- 复验日期：2026-09-08 至 2026-09-09（Asia/Shanghai）
- 候选版本：`1.0.0`
- 候选源码提交：`72f7e9201f9027b30479aacb63c20c280cd6438b`
- Marketplace ref：`marketplace/v1.0.0`
- Marketplace revision：`22602857f76a9fdf91c26a6ac6e57fa5a129992c`
- 绑定 evidence：[`evidence/m3-v1.0.0-final-candidate-2026-09-08/release-evidence.json`](evidence/m3-v1.0.0-final-candidate-2026-09-08/release-evidence.json)
- 当前结论：候选技术门禁、strict 聚合与独立最终只读审计 PASS；Private Vulnerability Reporting 已启用并复核，用户随后明确批准正式发布。本记录保留候选阶段证据。

## 1. 版本冻结与可复现候选

ADR 0028 将第一个公开稳定版本冻结为 `1.0.0`。候选不是把 `0.3.0-rc.6` 产物改名：Plugin manifest、Starter 配对、六个分发二进制、八条 Go build record、distribution manifest 和 archive 均从 clean `72f7e92` 重新产生。

A/B 使用独立 Go build cache 构建，Plugin bundle、distribution 和 remote Marketplace payload 逐文件一致。两侧都通过 distribution/plugin/archive/remote-marketplace verifier 和 trusted native CLI/runner smoke。

| 对象 | SHA-256 |
| --- | --- |
| `DISTRIBUTION-MANIFEST.json` | `f34a482d029b2e1063bf4a4ac44dcbfe5194d17a5363f8e0a651fb3aaa4bfa63` |
| `codex-game-atelier-1.0.0.tar.gz` | `ddc4f295e24758261420199699f8f4fd550a22ae7387a31b053e278009d7cc1d` |

最终 Plugin tree 不含项目内部 `AGENTS.md`，文本扫描没有具体模型 ID、Unity、旧 Foundry 名称或超出 v1 的支持声明。Windows/Linux 文件仍是 artifact-only inventory，不是原生支持。

## 2. 远程 Plugin 取得

全新隔离 `CODEX_HOME` 从 GitHub `marketplace/v1.0.0` sparse ref 注册 Marketplace 并安装 Plugin。安装 cache 与 bundle A 逐文件一致；public CLI 返回 `codex-game-atelier 1.0.0`，private runner 直接调用退出 `125`，Plugin tree 没有 `com.apple.quarantine`。

第一次不带命令级网络代理的远程取得在 90 秒内无输出，人工中断为退出 `130`，没有安装成功或被计为产品 FAIL。第二次使用当前机器已有的局域网代理 `192.168.5.88:7893` 作为单次进程环境后完成。没有写 Git/Codex/系统全局代理、没有修改系统设置、没有 Gatekeeper 干预，也没有移除 quarantine。该差异属于本机到 GitHub 的网络路由条件；Plugin 安装链本身没有产生阻断或额外手工步骤。

Marketplace CI run `34215969092` 在 revision `2260285` 上 PASS。该 ref 是已授权的候选测试 ref，不是受保护 `v1.0.0` tag、GitHub Release 或正式 Plugin 发布。

## 3. 远程安装包 Godot E2E

远程 cache 中的 CLI 在新目录 `.tools/M3 v1.0.0 最终冻结 #20260908/codex-game-atelier-starter` 创建项目；路径覆盖中文、空格和 `#`。Godot 为 `4.7.2.stable.official.ed1daf0bf` standard/GDScript。

| 检查 | 结果与证据 |
| --- | --- |
| Embedded Starter create | PASS；11 files、13,617 bytes、template `1.0.0` |
| Atomic initialize | PASS；project schema `1.0.0`、revision 0、standard mode |
| Headless validation | PASS；run `atelier-20260908t103621.959293000z-cb740f240f07`，8/8 |
| Fixed GDScript tests | PASS；run `atelier-20260908t103640.804960000z-7a4abee4063a`，6/6 |
| Debug build | PASS；run `atelier-20260908t103715.106420000z-b150691126ce` |
| Release build | PASS；run `atelier-20260908t103833.506582000z-71562d780764` |
| Direct Release export | PASS；run `atelier-20260908t103947.733488000z-17a4f339dd5f` |

| 产物 | SHA-256 | 大小 | 目标 smoke |
| --- | --- | ---: | --- |
| Debug build ZIP | `7a02ead387a9b8e197b3841a4bd66417f977be1d218f4fa22ee67b96dbf216e7` | 64,329,953 bytes | macOS arm64 headless one-frame，退出 0 |
| Release build ZIP | `ab52ad5a1cfc7ef9e114c5bb7e27bba0bdbe0a0c8de55b91f837e2f0a198c415` | 59,655,626 bytes | macOS arm64 headless one-frame，退出 0 |
| Direct Release export ZIP | `40f3a3f6ba8ffbbeace9faa0e11e71e4a4593cd489377b2a764e04f68b27a202` | 59,655,624 bytes | macOS arm64 headless one-frame，退出 0 |

三份 artifact 均验证 Universal 2 结构，但只在 Apple Silicon 实际启动。manifest 明确 `unsigned=true`、`not_notarized=true`、`public_distribution_ready=false`；这符合 Plugin-only v1 和“只验证 Apple Silicon 技术导出”的冻结边界。

预候选 `d93dee4` 曾在 Codex 文件沙箱内因 Godot 标准 `user://logs` 和系统 CA 访问被拒而提交 `FAIL/5 GODOT_REPORTED_ERRORS`；使用同一候选和声明过的 `--allow-engine-user-data`、仅放宽本次工具执行权限后 Headless 8/8 PASS。原始 Godot、普通副本与 ad-hoc snapshot 的对照确认这是工具沙箱权限边界，不是新的 CLI/Starter 回归。失败 run 保留，没有通过删除证据获得绿色结果。

## 4. 用户级生命周期与状态恢复

候选字节冻结后重新执行一次真实用户级生命周期：

1. 安装 previous version `0.3.0-rc.6`。
2. `rc.6 → 1.0.0` 升级，cache 与最终 bundle A 一致。
3. 从故意截断 manifest 的独立 Marketplace 执行无效升级；安装退出 `1`，`1.0.0` 保持 active 且 cache 未变化。
4. `1.0.0 → rc.6` 回滚，rc.6 cache 与其候选一致。
5. 再次安装 `1.0.0`，cache 与最终候选一致。
6. 登录态临时只读任务 `01a0809c-4a8c-7223-804a-e3e643ac2af6` 发现 `codex-game-atelier:develop-godot-game`、version `1.0.0` 和 `skills/develop-godot-game/SKILL.md`。
7. 卸载 Plugin，移除正式与无效 Marketplace。
8. 从演练前私有快照逐字节恢复 `~/.codex/config.toml`，并安全移除 Codex 卸载后留下的 Atelier 空 cache 父目录。

演练前后均为 14 个启用 Plugin、6 个 Marketplace、0 个 Atelier Plugin/Marketplace。Plugin ID hash 为 `88cc8c979f040b7b9774f4ab6198d0530610fecf6a389048ca5a413f87404341`，Marketplace 名称 hash 为 `fa7db3991654ad43637e2b48b948bcce8b340d10adf1dd84faf97bd04653ae2d`。配置 mode `0600`、5,955 bytes、SHA-256 `129d9bca33d8686a90b46c5e6c6f60e39ec77948ad1b0210bb19cb8e4ecd7327` 前后完全一致；`~/.codex/plugins/cache/codex-game-atelier` 最终不存在。

## 5. CI、分支保护与 strict 聚合

main CI run `34215199240` 在源码 `72f7e92` 上 PASS。现场只读 GitHub API 复核：`verify-macos-arm64` 仍是 strict required check，`enforce_admins=true`，force push 与 branch deletion 均关闭，required signatures 关闭。

最终远程安装项目先执行 standard，再用 distribution candidate A 和绑定 evidence 执行 strict：

| 模式 | run | 结果 |
| --- | --- | --- |
| standard | `atelier-20260909t041547.915710000z-676caef22554` | 6 PASS、0 BLOCKED、0 NOT_RUN；不冒充 strict readiness |
| strict | `atelier-20260909t041548.317782000z-d8c53749b438` | 12 PASS、0 BLOCKED、0 NOT_RUN；`release_ready=true` |

strict 输入路径在 stdout 只记录为 `provided`，命令没有修改项目或候选。

## 6. 回归与已知非产品失败

- PR #9 的两项 macOS Apple Silicon check 与合并后的 main CI 全部 PASS。
- Go：项目内 Go 1.27.0、`gofmt -l` 无差异、`go vet ./...` PASS、`go test -count=1 ./...` PASS。
- Python validators：55/55 PASS。
- Draft 2020-12：27 schemas、33 fixtures、11 persisted Starter records、13 persisted collaboration records、44 negative assertions PASS。
- A/B distribution、Plugin、archive、remote Marketplace verify 与 trusted native smoke 全部 PASS。

一次初始本地 Go 回归命令只为 vet/test 设置项目缓存，却让格式命令尝试默认系统 Go cache，因沙箱拒绝而在测试前退出；改用项目内绝对 `gofmt` 并对整条命令设置项目缓存后 PASS。一次把完整 Git worktree 交给“只接受生成态 Marketplace 根”的 verifier，按顶层 allowlist 正确拒绝；对生成态 payload 验证并与 worktree 的 `.agents`/`plugins` 子树逐文件比较后 PASS。这两项是验证调用错误，不覆盖或删除。

## 7. 尚未完成与发布边界

- 独立架构、安全、许可与发布最终只读审计已 PASS，最终为 0 Blocker、0 High、0 Medium、0 Low；V1-11 更新为 PASS。详见 [`m3-v1.0.0-final-readonly-audit-2026-09-09.md`](m3-v1.0.0-final-readonly-audit-2026-09-09.md)。
- GitHub Private Vulnerability Reporting 在候选审计时只读返回 `enabled=false`；2026-09-09 经用户明确授权后已启用，随后 GitHub API 复核返回 `enabled=true`。该后续仓库设置不改变候选源码、字节或 hash。
- V1-12 用户于 2026-09-09 明确批准正式发布；正式 Plugin 使用受保护 `v1.0.0` tag。
- 未创建 GitHub Release，未执行 npm publish、签名或公证。
- Windows/Linux 原生验证、macOS Intel smoke、第二 UID/机器、npm、standalone/DMG/PKG 均不属于冻结 v1 门禁。
